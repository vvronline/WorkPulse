/**
 * MOVED → src/calls/p2p/callStateMachine.ts (calls-module separation).
 * This shim preserves the historical import path; new code should import
 * from "../calls/p2p/callStateMachine" directly.
 */
export * from "../calls/p2p/callStateMachine";