<!--
  Android notification SMALL icon (status-bar / notification-tray).

  WHY THIS FILE EXISTS:
  An Android notification's small icon MUST be a STATIC, compile-time `drawable`
  resource. The OS renders it as a flat, single-colour SILHOUETTE (it strips all
  colour and tints it), so it must be a white-on-transparent shape. Critically,
  a notification posted with NO resolvable small icon is DROPPED SILENTLY — the
  channel sound still plays (the "ding") but NO status-bar/lockscreen entry ever
  appears. This was the root cause of "messages: sound but no notification":
  `notifeeService.displayMessage` pointed `smallIcon` at "ic_launcher", which
  only exists as an ADAPTIVE `mipmap` (rejected by Android as a small icon).

  This dedicated monochrome chat-bubble glyph is a real `drawable` so Notifee can
  always resolve it. The runtime org-branding logo (dynamic, full-colour) cannot
  be the small icon for these reasons — it is used as the notification LARGE icon
  instead (see notifeeService.displayMessage).

  Copied into android/app/src/main/res/drawable by the
  `withAndroidNotificationIcon` config plugin at prebuild (and committed directly
  into res/drawable so the already-prebuilt project works without a re-prebuild).
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24">
    <path
        android:fillColor="#FFFFFFFF"
        android:pathData="M20,2H4C2.9,2 2,2.9 2,4v18l4,-4h14c1.1,0 2,-0.9 2,-2V4C22,2.9 21.1,2 20,2zM7,9h10v2H7V9zM13,14H7v-2h6V14zM17,8H7V6h10V8z" />
</vector>