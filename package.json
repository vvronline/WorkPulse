{
  "name": "workpulse",
  "version": "1.0.0",
  "private": true,
  "description": "AINO monorepo — desktop + mobile + server + client",
  "scripts": {
    "release": "pwsh ./scripts/release.ps1"
  }
}