FROM edoburu/pgbouncer:v1.24.1-p1

# The upstream image renders config from environment variables. Railway injects
# DB_* references to the managed Postgres service; see this directory's README.
EXPOSE 5432